#include<bits/stdc++.h>
using namespace std;

void propagate(){
	
}

void update(......., int Qleft, int Qright){

	if(){
	
		add[]+=x;
	}
	
	update();
	update();
	tree[parent]= tree[lc]+tree[rc] + add[lc]*range[lc] + add[rc]*range[rc];
	
	

}

int query(){
	if(){
		return tree[]+range[]*add[];
	}
	propagate();
}
